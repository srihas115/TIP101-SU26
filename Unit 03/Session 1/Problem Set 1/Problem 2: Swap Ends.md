# Problem 2: Swap Ends

Write a function `swap_ends()` that accepts a string `my_str` as a parameter and returns a new string where the first and last characters from `my_str` are swapped.


```python
def swap_ends(my_str):
    pass
```

Example Usage:


```python
my_str = "boat"
swapped = swap_ends(my_str)
print(swapped)
```

Example Output: `toab`


<details>
  <summary>✨ <b>AI Hint: String Indexing</b></summary>

  <br>

*Key Skill: Use AI to explain code concepts*


This problem requires you to be familiar with indexing characters in a string. In Python, we can use square brackets to index and slice subsections of a string in the same way we would index or slice a list.


If you'd still like to see more examples or ask follow-up questions, try using an AI tool like ChatGPT or GitHub Copilot. You can use the following prompt as a starting point:


*"You're an expert computer science tutor. Please provide an examples of string indexing in Python, and explain how it compares to list indexing."*
</details>
### 💡 What's the difference between strings and lists?


In many ways, Python lists and strings are very similar, but they also have some key differences. The difference between strings and lists is not strictly required to solve the exercises in this problem set, however we highly recommend you take a moment to explore the difference between the two data types when you have time.


For your convenience, we've included a section comparing strings and lists in the [Unit 3 Cheatsheet](https://courses.codepath.org/courses/tip101/unit/3#!cheatsheet), and as always you should practice your independent research skills by using search engines and AI tools to dive deeper!
