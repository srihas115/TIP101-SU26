# Problem 3: List Concatenation

Given an integer list `nums` of length `n`, create a function `concatenate_list()` that creates and returns a list `ans` of length `2n` where `ans[i] == nums[i]` and `ans[i + n] == nums[i]` for `0 <= i < n` (**0-indexed**).

Specifically, `ans` is the **concatenation** of two `nums` lists.


```python
def concatenate_list(nums):
    pass
```

Example Input: `[1,2,3,4]`

Example Output: `[1,2,3,4,1,2,3,4]`


<details>
  <summary>✨ <b>AI Hint: Encountering New Vocabulary</b></summary>

  <br>

*Key Skill: Use AI to explain code concepts*


Oftentimes as we learn a new language or software skill, we encounter new terms we're unfamiliar with. Part of the *Understand* step of solving a coding question is to find definitions for any terms we don't know! If you're unfamiliar with the term `concatenation` or simply don't know how to concatenate a list in Python, try using a web browser or AI tool like ChatGPT to look it up!


*"You're an expert computer science tutor. I am a beginner programmer and Python student. Please provide a simple one sentence definition of 'concatenation' and explain how to concatenate a list in Python. "*
</details>
