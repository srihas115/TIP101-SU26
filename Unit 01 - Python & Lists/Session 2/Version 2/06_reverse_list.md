# Problem 6: Reverse List

Write a function `reverse_list()` that takes in a list `lst` as a parameter and returns a new list containing the elements of `lst` in reverse order.


```python
def reverse_list(lst):
    pass
```

Example Usage:


```python
lst = [1,2,3,4,5]
rev_lst = reverse_list(lst)
print(rev_lst)
```

Example Output:


```
[5,4,3,2,1]
```


<details>
  <summary>✨ <b>AI Hint: Reversing a list</b></summary>

  <br>

*Key Skill: Use AI to explain code concepts*


There are many ways to approach this problem. If you're struggling with the *P-lan* step in braimstorming your approach, try using a web browser or AI tool like ChatGPT or GitHub Copilot to research different list methods and string slicing techniques that may help. You may also consider researching how to use the `range()` function to iterate backwards through a list.
</details>
