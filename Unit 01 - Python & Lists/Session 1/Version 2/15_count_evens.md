# Problem 15: Count Evens

Write a function `count_evens()` that takes in a list of integers `lst` as a parameter. The function returns the number of even numbers in the list.


```python
def count_evens(lst):
    pass
```

Example Usage:


```python
lst1 = [1,5,7,9]
count1 = count_evens(lst1)
print(count1)

lst2 = [2,4,6,8]
count2 = count_evens(lst2)
print(count2)
```

Example Output:


```python
0
4
```


### 💡 Remainders with Modulus Division


This problem requires you to know how to find the remainder of a division operation. We can do this with something called modulus division. Check out this resource on how we can achieve this with the modulo operator in Python: [Modular Divison - GeeksforGeeks](https://www.geeksforgeeks.org/what-is-a-modulo-operator-in-python/).
