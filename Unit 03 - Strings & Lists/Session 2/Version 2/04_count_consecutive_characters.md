# Problem 4: Consecutive Characters

Write a function `count_consecutive_characters()` that takes in a string `str1` as a parameter and returns the count of the most frequent consecutive character.


```python
def count_consecutive_characters(str1):
    pass
```

Example Usage:


```python
str1 = "aaabbcaaaa"
count = count_consecutive_characters(str1)
print(count)
str2 = "abcde"
count2 = count_consecutive_characters(str2)
print(count2)
```

Example Output:


```
4
1
```
